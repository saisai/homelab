# create user name
```
sudo adduser username
```

To grant administrative (sudo) privileges (Recommended): <br />
```
sudo usermod -aG sudo username
```


Verification
```
groups username
```